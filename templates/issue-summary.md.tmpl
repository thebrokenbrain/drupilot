<!--
  drupilot — Drupal.org issue summary for a Drupal 11 compatibility port.
  Rendered by scripts/contrib/make-issue.sh ({{TOKENS}} substituted from the
  port context). Paste the rendered output into the issue's "Issue summary"
  field on drupal.org (it accepts this HTML, and the anchor ids match the
  project's standard issue template so the summary tooling links correctly).

  Only the sections that apply to a behavior-preserving port are included.
  Steps to reproduce, User interface changes, API changes and Data model
  changes are intentionally omitted — a Phase 1 minimal port introduces none.
-->
<h3 id="summary-problem-motivation">Problem/Motivation</h3>

{{PROBLEM_MOTIVATION}}

<h3 id="summary-proposed-resolution">Proposed resolution</h3>

{{PROPOSED_RESOLUTION}}

<h3 id="summary-remaining-tasks">Remaining tasks</h3>

{{REMAINING_TASKS}}
