# Drupal 11 Viability Report — {{SUBJECT_NAME}}

> Generated by drupilot on {{DATE}}. This report is an estimate based on static
> analysis (Rector dry-run, PHPStan, PHPCS, and Upgrade Status when Drupal is
> installed). No files were modified.

## Subject

| Field | Value |
|---|---|
| Name | `{{SUBJECT_NAME}}` |
| Machine name | `{{SUBJECT_MACHINE_NAME}}` |
| Type | {{SUBJECT_TYPE}} (module / theme / profile) |
| Path | `{{SUBJECT_PATH}}` |
| Current `core_version_requirement` | `{{CURRENT_CORE_REQUIREMENT}}` |

## Targets

| Field | Value |
|---|---|
| Drupal target | `{{DRUPAL_TARGET}}` |
| PHP target | `{{PHP_TARGET}}` {{PHP_TARGET_NOTE}} |
| Keep Drupal 10 compatible | {{KEEP_D10}} (`^10 \|\| ^11` vs `^11`) |

## Verdict

**Effort estimate: {{VERDICT}}** (S / M / L / XL)

{{VERDICT_SUMMARY}}

> Threshold check: configured threshold is `{{VIABILITY_THRESHOLD}}`.
> {{THRESHOLD_NOTE}}
> drupilot does not refuse large refactors — even past the threshold it delivers
> a staged porting plan (see `port-plan.md`) that preserves the original
> functionality without colliding with Drupal 11.

## Change volume

| Category | Count |
|---|---|
| Auto-fixable by Rector (official) | {{RECTOR_AUTOFIX_COUNT}} |
| Auto-fixable by digests (complementary, AI-generated) | {{DIGESTS_AUTOFIX_COUNT}} |
| Manual changes required | {{MANUAL_COUNT}} |
| PHPStan deprecation findings (level {{PHPSTAN_LEVEL}}) | {{PHPSTAN_COUNT}} |
| PHPCS violations (Drupal, DrupalPractice) | {{PHPCS_COUNT}} |

## Hard breaks detected

These are migrations that Rector cannot fully automate and that typically need
human review:

| Break | Status | Notes |
|---|---|---|
| Twig 3 (removed filters/functions, `spaceless`) | {{TWIG3_STATUS}} | {{TWIG3_NOTES}} |
| CKEditor 5 (CKEditor 4 removed since D10) | {{CKEDITOR5_STATUS}} | {{CKEDITOR5_NOTES}} |
| jQuery / jQuery UI (`core/jquery.ui.*` removed/externalized) | {{JQUERY_STATUS}} | {{JQUERY_NOTES}} |
| Symfony 7 (event subscriber signatures / typing) | {{SYMFONY7_STATUS}} | {{SYMFONY7_NOTES}} |

## `info.yml` status

{{INFO_YML_STATUS}}

- Required minimal change: `core_version_requirement: {{TARGET_CORE_REQUIREMENT}}`.
- A missing `core_version_requirement` (or the legacy `core: 8.x` key) is blocking.

## Contrib dependencies — Drupal 11 status

| Dependency | Drupal 11 support | Notes |
|---|---|---|
{{CONTRIB_DEPENDENCY_ROWS}}

## Phased plan (summary)

A detailed, staged plan is in `port-plan.md`. At a glance:

1. **Phase 1 — Minimal compatibility (default):** {{PHASE1_SUMMARY}}
2. **Phase 2 — Drupal 11 way refactor (opt-in):** {{PHASE2_SUMMARY}}

## Recommended next step

{{NEXT_STEP}}

---

## Appendix — raw tool output

<details>
<summary>Rector (official, dry-run)</summary>

```
{{RECTOR_RAW_OUTPUT}}
```
</details>

<details>
<summary>Rector (digests / complementary, dry-run)</summary>

```
{{DIGESTS_RAW_OUTPUT}}
```
</details>

<details>
<summary>PHPStan</summary>

```
{{PHPSTAN_RAW_OUTPUT}}
```
</details>

<details>
<summary>PHPCS (Drupal, DrupalPractice)</summary>

```
{{PHPCS_RAW_OUTPUT}}
```
</details>

<details>
<summary>Upgrade Status (only if Drupal is installed)</summary>

```
{{UPGRADE_STATUS_RAW_OUTPUT}}
```
</details>
